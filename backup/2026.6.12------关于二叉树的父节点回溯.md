``StringBuilder path = StringBuilder();
int len = path.length(); //用来记录当前长度，如1->2
if(root.left == null && root.right == null){
      ans.add(path.toString());
 ｝else {
                path.append("->");
                dfs(root.left,path,ans);
                dfs(root.right,path,ans);
｝
      path.setLength(len); //回溯到父节点的，接上如1->